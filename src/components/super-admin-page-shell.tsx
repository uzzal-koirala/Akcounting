import { Construction } from "lucide-react";

export function SuperAdminPageShell({ eyebrow, title, description }: { eyebrow: string; title: string; description: string }) {
  return <div><header className="flex flex-col gap-4 border-b border-slate-200 pb-6 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-[9px] font-semibold uppercase tracking-[0.18em] text-violet-600">{eyebrow}</p><h1 className="mt-2 text-2xl font-semibold tracking-tight text-slate-950">{title}</h1><p className="mt-1.5 text-[10px] text-slate-500">{description}</p></div><div className="flex items-center gap-2"><button className="grid size-10 place-items-center rounded-lg border border-slate-200 bg-white text-slate-500 shadow-sm"><BellIcon /></button><div className="rounded-lg border border-slate-200 bg-white px-3 py-2 shadow-sm"><p className="text-[9px] font-semibold text-slate-700">Platform status</p><p className="mt-0.5 flex items-center gap-1.5 text-[8px] text-emerald-600"><span className="size-1.5 rounded-full bg-emerald-500" />All systems operational</p></div></div></header><section className="mt-6 grid min-h-[470px] place-items-center rounded-2xl border border-dashed border-violet-200 bg-white/70 p-8 text-center"><div><span className="mx-auto grid size-12 place-items-center rounded-xl bg-violet-50 text-violet-600"><Construction size={20} /></span><h2 className="mt-4 text-sm font-semibold text-slate-800">{title} page is ready</h2><p className="mx-auto mt-2 max-w-sm text-[10px] leading-5 text-slate-400">The route and admin layout are prepared. We can design the detailed content and actions for this page next.</p></div></section></div>;
}

function BellIcon() { return <span className="relative"><span className="block size-2 rounded-full border-2 border-slate-500" /><span className="absolute -right-1 -top-1 size-1.5 rounded-full bg-violet-600 ring-1 ring-white" /></span>; }
