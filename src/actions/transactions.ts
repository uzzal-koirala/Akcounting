"use server";
// Add validated transaction mutations here when the database schema is connected.
export async function createTransaction() { throw new Error("Transaction persistence is not configured yet."); }
